## Processamento de imagem Python

### Conceitos Básicos

1. Cor. Se você já pintou com pigmentos, sabe que misturar tinta vermelha, amarela e azul pode produzir outras cores. Na arte, estas são as três cores primárias, o que significa que não podem ser mais subdivididas. Nos computadores, entretanto, formamos cores combinando a luz vermelha, verde e azul em diferentes proporções, portanto, essas são as três cores primárias aditivas da luz. Em sistemas de computador, geralmente representamos uma cor como um valor RGB ou um valor RGBA, onde `A` representa o canal alfa, que determina a transparência.

    | Nome | Valor RGB | Nome | Valor RGB |
   | :---------: | :-------------: | :----------: | :-----------: |
    | Branco (branco) | `(255, 255, 255)` | Vermelho (vermelho) | `(255, 0, 0)` |
    | Verde (verde) | `(0, 255, 0)` | Azul (azul) | `(0, 0, 255)` |
    | Cinza (cinza) | `(128, 128, 128)` | Amarelo (amarelo) | `(255, 255, 0)` |
    | Preto (preto) | `(0, 0, 0)` | Roxo (roxo) | `(128, 0, 128)` |

2. Pixel. Para uma imagem representada por uma sequência digital, a menor unidade é o pequeno quadrado de uma única cor da imagem. Todos esses pequenos quadrados têm uma posição clara e um valor de cor atribuído, e a cor e a posição desses pequenos quadrados decidem a aparência final da imagem. São unidades indivisíveis e normalmente os chamamos de pixels. Cada imagem contém um certo número de pixels, e esses pixels decidem o tamanho que a imagem mostra na tela. Para quem gosta de tirar fotos ou selfies, o termo pixel não é novidade.

### Processando imagens com Pillow

Pillow é um ramo que evoluiu da PIL, a famosa biblioteca de processamento de imagens Python. Com o Pillow, podemos realizar compactação de imagens e muitos outros tipos de manipulação de imagens. Instale o Pillow com o comando abaixo.

```bash
pip install pillow
```

O mais importante no Pillow é a classe `Image`. Podemos usar a função `open` do módulo `Image` para ler uma imagem e obter um objeto `Image`. Os exemplos abaixo geram as imagens de que precisam, portanto não dependem de fotos ou fontes que não fazem parte do repositório.

#### 1. Criar e exibir uma imagem

```python
from PIL import Image

# Cria uma imagem e a salva para também demonstrar open().
Image.new('RGB', (500, 750), color=(135, 206, 235)).save('demo.png')
# Lê a imagem e obtém um objeto Image.
image = Image.open('demo.png')
# Obtém o formato, tamanho e modo da imagem.
print(image.format)  # PNG
print(image.size)    # (500, 750)
print(image.mode)    # RGB
image.show()
```

#### 2. Cortar uma imagem

```python
from PIL import Image

image = Image.new('RGB', (500, 750), color=(135, 206, 235))
# Recorta a imagem na região especificada.
image.crop((80, 20, 310, 360)).show()
```

#### 3. Gerar uma miniatura

```python
from PIL import Image

image = Image.new('RGB', (500, 750), color=(135, 206, 235))
# Gera uma miniatura com o tamanho especificado.
image.thumbnail((128, 128))
image.show()
```

#### 4. Redimensionar e colar uma imagem

```python
from PIL import Image

background = Image.new('RGB', (500, 750), color=(240, 240, 240))
source = Image.new('RGB', (500, 750), color=(135, 206, 235))
# Recorta e redimensiona parte da imagem antes de colá-la no fundo.
cropped = source.crop((80, 20, 310, 360))
width, height = cropped.size
background.paste(cropped.resize((int(width / 1.5), int(height / 1.5))), (172, 40))
background.show()
```

#### 5. Girar e inverter

```python
from PIL import Image

image = Image.new('RGB', (500, 750), color=(135, 206, 235))
# Gira a imagem.
image.rotate(45).show()
# Image.Transpose.FLIP_TOP_BOTTOM representa a inversão vertical.
image.transpose(Image.Transpose.FLIP_TOP_BOTTOM).show()
```

#### 6. Manipular pixels

```python
from PIL import Image

image = Image.new('RGB', (500, 750), color=(135, 206, 235))
for x in range(80, 310):
    for y in range(20, 360):
        # Modifica o pixel especificado.
        image.putpixel((x, y), (128, 128, 128))
image.show()
```

#### 7. Efeitos de filtro

```python
from PIL import Image, ImageFilter

image = Image.new('RGB', (500, 750), color=(135, 206, 235))
# ImageFilter contém filtros internos; filtros personalizados também são possíveis.
image.filter(ImageFilter.CONTOUR).show()
```

### Desenhando com Pillow

O Pillow inclui um módulo denominado `ImageDraw`. A função `Draw` nesse módulo retorna um objeto `ImageDraw`. Através desse objeto, métodos como `arc`, `line`, `rectangle`, `ellipse` e `polygon` podem ser usados para desenhar arcos, linhas, retângulos, elipses e polígonos em uma imagem, e o método `text` pode ser usado para adicionar texto.

Para desenhar uma imagem como o exemplo da lição, o código completo é mostrado abaixo.

```python
import random

from PIL import Image, ImageDraw, ImageFont


def random_color():
    """Generate a random color"""
    red = random.randint(0, 255)
    green = random.randint(0, 255)
    blue = random.randint(0, 255)
    return red, green, blue


width, height = 800, 600
# Create an 800x600 image with a white background
image = Image.new(mode='RGB', size=(width, height), color=(255, 255, 255))
# Create an ImageDraw object
drawer = ImageDraw.Draw(image)
# Use Pillow's built-in font so no external font file is required
font = ImageFont.load_default()
# Draw text
drawer.text((300, 50), 'Hello, world!', fill=(255, 0, 0), font=font)
# Draw two diagonal lines
drawer.line((0, 0, width, height), fill=(0, 0, 255), width=2)
drawer.line((width, 0, 0, height), fill=(0, 0, 255), width=2)
xy = width // 2 - 60, height // 2 - 60, width // 2 + 60, height // 2 + 60
# Draw a rectangle
drawer.rectangle(xy, outline=(255, 0, 0), width=2)
# Draw ellipses
for i in range(4):
    left, top, right, bottom = 150 + i * 120, 220, 310 + i * 120, 380
    drawer.ellipse((left, top, right, bottom), outline=random_color(), width=8)
# Show the image
image.show()
# Save the image
image.save('result.png')
```

### Resumo

Ao usar a linguagem Python para desenvolvimento, além de usar Pillow para processar imagens, também podemos usar a biblioteca OpenCV mais poderosa para completar o processamento de imagens. OpenCV, Open Source Computer Vision Library, é uma biblioteca de visão computacional multiplataforma. Ele pode ser usado para desenvolver programas de processamento de imagens em tempo real, visão computacional e reconhecimento de padrões. Em nosso trabalho diário, muitas tarefas tediosas e enfadonhas podem ser realizadas por meio de programas Python. O objetivo da programação é permitir que o computador nos ajude a resolver problemas e reduzir o trabalho chato e repetido. Ao aprender este capítulo, acredito que todos já sentiram a diversão de desenhar e editar imagens com programas Python. Na verdade, Python pode fazer muito mais do que isso. Continue seu aprendizado.
