# Ficando confortável com o sistema operacional Linux

> **Observação**: A explicação dos comandos do Linux nesta lição é baseada em uma distribuição Linux chamada CentOS. Usei um servidor Alibaba Cloud e a versão do sistema era CentOS Linux versão 7.6.1810. Diferentes distribuições do Linux têm algumas pequenas diferenças em comandos shell e programas utilitários, mas essas diferenças são muito pequenas.

### História dos Sistemas Operacionais

Um sistema de computador apenas com hardware e sem software é chamado de "máquina nua". É difícil para nós usar uma máquina simples para realizar o trabalho diário do computador, como armazenamento e computação, por isso devemos usar um software especial para controlar o hardware. O software mais próximo do hardware do computador é o software do sistema, e a parte mais importante dele é o sistema operacional. Um sistema operacional é uma coleção de programas que controla e gerencia todos os recursos de hardware e software do computador, realiza a alocação de recursos e agendamento de tarefas e fornece interfaces e um ambiente para usuários do sistema e outros softwares.

#### Sem sistema operacional (operação manual)

No início, antes da existência dos sistemas operacionais, as pessoas primeiro carregavam uma fita de papel ou cartão de programa no computador, depois iniciavam o dispositivo de entrada para enviar o programa para o computador e, em seguida, iniciavam o programa usando os interruptores do console. Ao final da execução do programa, a impressora imprimia o resultado e o usuário descarregava a fita de papel ou os cartões e os retirava. Em seguida, o segundo usuário utilizou o computador e repetiu os mesmos passos. Durante todo o processo, o usuário utilizou a máquina sozinho, a CPU esperou pela operação manual e o uso de recursos foi muito baixo.

#### Sistemas em lote

Primeiro, inicie um programa de supervisão no computador. Sob o controle deste programa de supervisão, o computador pode processar automaticamente e em lotes os trabalhos de um ou mais usuários. Após a conclusão de um lote de trabalhos, o programa de supervisão lê novamente os trabalhos do dispositivo de entrada e os armazena na unidade de fita. Em seguida, repete as mesmas etapas para processar tarefas. O programa de supervisão continua processando os trabalhos e faz com que ocorra a transferência automática dos trabalhos, o que reduz o tempo de configuração dos trabalhos e o tempo de operação manual, além de melhorar o uso dos recursos do computador. Os sistemas em lote podem ser divididos em sistemas em lote de programa único, sistemas em lote multiprogramas, sistemas em lote on-line e sistemas em lote off-line.

#### Sistemas de compartilhamento de tempo e sistemas em tempo real

Um sistema de compartilhamento de tempo divide o tempo de execução do processador em intervalos de tempo muito curtos e atribui ao processador cada tarefa on-line, por sua vez, de acordo com o intervalo de tempo. Se um trabalho não puder terminar seu cálculo dentro do intervalo de tempo, o trabalho será pausado por um tempo, o processador será atribuído a outro trabalho e o trabalho original aguardará a próxima rodada de agendamento para continuar em execução. Como o computador é rápido e os trabalhos se revezam muito rapidamente, cada usuário sente que tem um computador só para si. Cada usuário pode enviar diferentes comandos de controle de operação ao sistema através de seu próprio terminal e finalizar tarefas em um ambiente totalmente interativo homem-computador. Para resolver o problema de que os sistemas de tempo compartilhado não conseguiam responder aos comandos do usuário a tempo, surgiram os sistemas de tempo real. Esses sistemas podem concluir o processamento de eventos dentro de um intervalo de tempo estrito e responder a tempo a eventos externos aleatórios.

#### Sistemas operacionais de uso geral

1. Na década de 1960, a série System/360 da IBM tinha um sistema operacional unificado chamado OS/360.
2. Em 1965, o Bell Labs da AT&T juntou-se à GE e ao MIT no projeto MULTICS.
3. Em 1969, o projeto MULTICS falhou. Ken Thompson estava em casa e, para jogar o jogo *Viagem Espacial*, usou linguagem assembly para desenvolver Unics no já desatualizado PDP-7.

   > **Nota**: É difícil imaginar que um sistema operacional tão bom como o Unix tenha sido desenvolvido por um programador que ficou em casa, em uma máquina desatualizada, apenas para jogar.

4. Em 1970 e 1971, Ken Thompson e Dennis Ritchie reescreveram Unics na linguagem B no PDP-11 e, por sugestão de Brian Kernighan, renomearam-no como Unix.

   <img src="res/ken-and-dennis-pdp-11.png" style="zoom:62%;" />
5. Em 1972 e 1973, Dennis Ritchie inventou a linguagem C para substituir a linguagem B menos portátil e começou a reescrever o Unix em C.
6. Em 1974, o Unix lançou a versão 5, que foi quase inteiramente implementada em C.
7. Em 1979, a partir da versão 7 do Unix, a AT&T privatizou o Unix por meio de novos termos de licenciamento.
8. Em 1987, o professor Andrew S. Tanenbaum queria explicar os detalhes internos do sistema operacional aos alunos em sala de aula, então, para evitar disputas de direitos autorais, ele decidiu desenvolver ele mesmo um sistema operacional compatível com Unix, sem usar nenhum código-fonte da AT&T. Este sistema foi denominado Minix.

   <img src="res/andrew.jpg" style="zoom:50%;" />

9. Em 1991, enquanto Linus Torvalds estudava na Universidade de Helsinque, na Finlândia, ele tentou fazer algum trabalho de desenvolvimento no Minix. Mas como o Minix era apenas um sistema operacional para ensino e não era muito poderoso, Linus escreveu um driver de disco e um sistema de arquivos para que ele pudesse ler, gravar e baixar arquivos com mais facilidade no grupo de notícias e no sistema de correio da universidade. Essas coisas formaram a forma inicial do kernel do sistema Linux.

![](./res/linus.png)

A imagem abaixo é o mapa da família de sistemas operacionais Unix.

![](./res/history-of-unix.png)

### Visão geral do Linux

Linux é um sistema operacional de uso geral. Um sistema operacional é responsável pelo agendamento de tarefas, alocação de memória e manipulação de E/S de dispositivos periféricos. Um sistema operacional geralmente é composto de duas partes: o kernel (o programa principal que executa outros programas e gerencia dispositivos de hardware, como discos e impressoras) e os programas do sistema (drivers de dispositivos, bibliotecas de baixo nível, shell, programas de serviço e assim por diante).

O kernel Linux foi desenvolvido por Linus Torvalds e lançado em setembro de 1991. Como produto da era da Internet, o sistema operacional Linux foi desenvolvido em conjunto por muitos desenvolvedores ao redor do mundo e é um sistema operacional gratuito. Observe que liberdade e gratuidade não são a mesma ideia.

### Vantagens do Linux

1. É um sistema operacional de uso geral e não está vinculado a hardware específico.
2. Ele é escrito em C, possui forte portabilidade e interfaces de programação de kernel.
3. Ele oferece suporte a vários usuários e múltiplas tarefas e oferece suporte a um sistema de arquivos em camadas seguro.
4. Possui muitos programas práticos, funções de rede completas e documentos de suporte robustos.
5. Possui segurança confiável e boa estabilidade e é mais amigável para os desenvolvedores.

### Versões de distribuição Linux

1. [Chapéu Vermelho](https://www.redhat.com/en)
2. [Ubuntu](https://www.ubuntu.com/)
3. [CentOS](https://www.centos.org/)
4. [Fedora](https://getfedora.org/)
5. [Debian](https://www.debian.org/)
6. [openSUSE](https://www.opensuse.org/)

### Comandos Básicos

Os comandos Linux em sistemas Linux geralmente usam o formato mostrado abaixo.

```bash
command [named options] [target]
```

1. Obtenha informações de login: `w` / `who` / `last` / `lastb`

   ```bash
   [root ~]# w
    23:31:16 up 12:16,  2 users,  load average: 0.00, 0.01, 0.05
   USER     TTY      FROM             LOGIN@   IDLE   JCPU   PCPU WHAT
   root     pts/0    182.139.66.250   23:03    4.00s  0.02s  0.00s w
   jackfrue pts/1    182.139.66.250   23:26    3:56   0.00s  0.00s -bash
   [root ~]# who
   root     pts/0        2018-04-12 23:03 (182.139.66.250)
   jackfrued pts/1        2018-04-12 23:26 (182.139.66.250)
   [root ~]# last
   root     pts/0        117.136.63.184   Sun May 26 18:57   still logged in
   reboot   system boot  3.10.0-957.10.1. Mon May 27 02:52 - 19:10  (-7:-42)
   ...
   ```
2. Verifique o shell que você está usando: `ps`

Um shell também é chamado de programa shell. É o tradutor entre o usuário e o kernel do sistema operacional. Simplificando, é a interface de interação entre as pessoas e o computador. Em muitos sistemas Linux, o shell padrão é `bash` (`Bourne Again SHell`), porque pode usar a tecla Tab para completar comandos e caminhos, pode salvar o histórico de comandos, pode configurar variáveis ​​de ambiente de maneira conveniente e pode executar operações em lote.

3. Veja a descrição e localização de um comando: `whatis` / `which` / `whereis`

   ```bash
   [root ~]# whatis ps
   ps (1)        - report a snapshot of the current processes.
   [root ~]# whereis ps
   ps: /usr/bin/ps /usr/share/man/man1/ps.1.gz
   [root ~]# which python
   /usr/bin/python
   ```
4. Limpe o que é mostrado na tela: `clear`
5. Ver documentos de ajuda: `man` / `info` / `--help` / `apropos`

   ```bash
   [root@izwz97tbgo9lkabnat2lo8z ~]# ps --help
   Usage:
    ps [options]
   ...
   [root@izwz97tbgo9lkabnat2lo8z ~]# man ps
   PS(1)                                User Commands                                PS(1)
   NAME
          ps - report a snapshot of the current processes.
   ...
   ```

6. Visualize o sistema e o nome do host: `uname` / `hostname`

   ```bash
   [root@izwz97tbgo9lkabnat2lo8z ~]# uname
   Linux
   [root@izwz97tbgo9lkabnat2lo8z ~]# hostname
   izwz97tbgo9lkabnat2lo8z
   [root@iZwz97tbgo9lkabnat2lo8Z ~]# cat /etc/centos-release
   CentOS Linux release 7.6.1810 (Core)
   ```

7. Hora e data: `date` / `cal`

   ```bash
   [root@iZwz97tbgo9lkabnat2lo8Z ~]# date
   Wed Jun 20 12:53:19 CST 2018
   [root@iZwz97tbgo9lkabnat2lo8Z ~]# cal
         June 2018
   Su Mo Tu We Th Fr Sa
                   1  2
    3  4  5  6  7  8  9
   ...
   ```

8. Reinicie e desligue: `reboot` / `shutdown`

   ```bash
   [root ~]# shutdown -h +5
   Shutdown scheduled for Sun 2019-05-26 19:34:27 CST, use 'shutdown -c' to cancel.
   ...
   [root ~]# shutdown -r 23:58
   Shutdown scheduled for Sun 2019-05-26 23:58:00 CST, use 'shutdown -c' to cancel.
   ```
9. Sair do login: `exit` / `logout`
10. Ver histórico de comandos: `history`

   ```bash
   [root@iZwz97tbgo9lkabnat2lo8Z ~]# history
   ...
   452  ls
   453  cd Python-3.6.5/
   454  clear
   455  history
   [root@iZwz97tbgo9lkabnat2lo8Z ~]# !454
   ```

### Programas utilitários

#### Operações de arquivos e diretórios

1. Crie e exclua diretórios vazios: `mkdir` / `rmdir`
2. Crie e exclua arquivos: `touch` / `rm`

   - `touch` é usado para criar um arquivo vazio ou alterar os horários dos arquivos. No Linux, um arquivo possui três vezes:
     - time when content is changed: `mtime`
     - time when permissions are changed: `ctime`
     - last access time: `atime`
   - Opções importantes de `rm`:
     - `-i`: interactive delete. It asks about every item.
     - `-r`: delete directories and recursively delete files and directories in them.
     - `-f`: force delete. Ignore files that do not exist and show no prompt.

3. Alterne e visualize o diretório de trabalho atual: `cd` / `pwd`

   > **Nota**: O comando `cd` pode ser seguido por um caminho relativo (usando o caminho atual como referência) ou um caminho absoluto (começando com `/`) para alternar para o diretório especificado. Você também pode usar `cd ..` para voltar ao diretório de nível superior.

4. Ver conteúdo do diretório: `ls`

   - `-l`: visualize arquivos e diretórios em formato longo
   - `-a`: mostra arquivos e diretórios que começam com ponto (arquivos ocultos)
   - `-R`: quando houver um diretório, continue expandindo-o recursivamente
   - `-d`: lista apenas diretórios, não outro conteúdo
   - `-S` / `-t`: classificar por tamanho / hora

5. Ver conteúdo do arquivo: `cat` / `tac` / `head` / `tail` / `more` / `less` / `rev` / `od`

   > **Nota**: O comando `wget` é usado acima. É um programa de download de rede que pode baixar recursos de um URL especificado.

6. Copie e mova arquivos: `cp` / `mv`
7. Renomear arquivos: `rename`
8. Encontre arquivos e pesquise conteúdo: `find` / `grep`

   > **Observação**: `grep` pode usar expressões regulares ao pesquisar strings. Se precisar de expressões regulares, você pode usar `grep -E` ou usar diretamente `egrep`.

9. Criar links e visualizar links: `ln` / `readlink`

   ```bash
   [root@iZwz97tbgo9lkabnat2lo8Z ~]# ls -l sohu.html
   -rw-r--r-- 1 root root 212131 Jun 20 19:15 sohu.html
   [root@iZwz97tbgo9lkabnat2lo8Z ~]# ln -s /etc/centos-release sysinfo
   [root@iZwz97tbgo9lkabnat2lo8Z ~]# cat sysinfo
   CentOS Linux release 7.4.1708 (Core)
   ```

   > **Observação**: Os links podem ser divididos em links físicos e links virtuais (links simbólicos). Um link físico pode ser considerado um ponteiro para dados de arquivo, como a contagem de referência de um objeto em Python. Cada vez que um link físico é adicionado, a contagem de links do arquivo aumenta em 1. Somente quando a contagem de links chega a 0 o espaço de armazenamento desse arquivo pode ser coberto por outros arquivos. Quando normalmente excluímos um arquivo, não estamos realmente excluíndo os dados do disco rígido. Excluímos apenas um ponteiro, ou podemos dizer um registro de uso dos dados. Um link virtual é semelhante a um atalho no Windows. Quando o arquivo vinculado pelo link virtual é excluído, o link virtual também se torna inválido.

10. Compactar e descompactar, arquivar e desarquivar: `gzip` / `gunzip` / `xz`

   ```bash
   [root@iZwz97tbgo9lkabnat2lo8Z ~]# wget http://download.redis.io/releases/redis-4.0.10.tar.gz
   ...
   [root@iZwz97tbgo9lkabnat2lo8Z ~]# gunzip redis-4.0.10.tar.gz
   [root@iZwz97tbgo9lkabnat2lo8Z ~]# ls redis*
   redis-4.0.10.tar
   ```

11. Arquivar e desarquivar: `tar`

   ```bash
   [root@iZwz97tbgo9lkabnat2lo8Z ~]# tar -xvf redis-4.0.10.tar
   redis-4.0.10/
   redis-4.0.10/.gitignore
   ...
   ```

   > **Observação**: Tanto o arquivamento (também chamado de criação de um arquivo) quanto o desarquivamento usam o comando `tar`. Normalmente, a criação de um arquivo precisa das três opções `-cvf`. Aqui, `c` significa criar, `v` significa mostrar detalhes detalhados e `f` significa o arquivo do arquivo. O desarquivamento precisa de `-xvf`, onde `x` significa extrair, e as outras duas opções são as mesmas da criação de um arquivo.

12. Transforme a entrada padrão em argumentos de linha de comando: `xargs`

Este comando é frequentemente usado em pipes e redirecionamentos.

13. Mostre o nome do arquivo ou nome do diretório em um caminho: `basename` / `dirname`
14. Outras ferramentas relacionadas:

   - `sort`: classificar conteúdo
   - `uniq`: remover duplicatas adjacentes
   - `tr`: substitua o conteúdo especificado por novo conteúdo
   - `cut` / `paste`: recortar e colar colunas de conteúdo
   - `split`: dividir arquivos
   - `file`: determinar o tipo de arquivo
   - `wc`: contar linhas, palavras e bytes
   - `iconv`: converter codificações

   ```bash
   [root ~]# cat foo.txt
   grape
   apple
   pitaya
   [root ~]# cat bar.txt
   100
   200
   300
   400
   [root ~]# paste foo.txt bar.txt
   grape   100
   apple   200
   pitaya  300
           400
   ...
   ```

#### Pipes e Redirecionamento

1. Usando um tubo, `|`

Exemplo: conte o número de arquivos no diretório atual.

   ```bash
   find ./ | wc -l
   ```

Exemplo: liste arquivos e diretórios no caminho atual e adicione um número a cada item.

   ```bash
   ls | cat -n
   ```

Exemplo: contar registros em `record.log` que contêm `AAA`, mas não contêm `BBB`.

   ```bash
   cat record.log | grep AAA | grep -v BBB | wc -l
   ```

2. Redirecionamento de saída e erro: `>` / `>>` / `2>`
3. Redirecionamento de entrada: `<`
4. Redirecionamento múltiplo: `tee`

O comando a seguir mostra o resultado de `ls` no terminal e o anexa a `ls.txt`.

   ```bash
   ls | tee -a ls.txt
   ```

#### Aliases

1. `alias`
2. `unalias`

Os aliases são convenientes para mapear combinações de comandos comumente usadas para nomes curtos.

```bash
[root ~]# alias ll='ls -l'
[root ~]# alias frm='rm -rf'
[root ~]# unalias frm
```

#### Processamento de texto

1. Editor de fluxo: `sed`

`sed` é uma ferramenta para operar, filtrar e transformar texto. Ele pode anexar linhas, inserir linhas, excluir linhas e fazer substituições. Como muitos comandos mostrados anteriormente, ele não modifica o arquivo original, a menos que você redirecione ou salve explicitamente o resultado.

   ```bash
   [root ~]# cat -n fruit.txt
        1  banana
        2  grape
        3  apple
        4  watermelon
        5  orange
   [root ~]# sed '2a pitaya' fruit.txt
   banana
   grape
   pitaya
   apple
   watermelon
   orange
   ```

2. Linguagem de digitalização e processamento de padrões: `awk`

`awk` é uma linguagem de programação e uma das ferramentas de processamento de texto mais poderosas do Linux. Ele pode extrair colunas específicas do texto, usar expressões regulares para recuperar conteúdo, exibir linhas especificadas e realizar estatísticas e cálculos. Resumindo, é extremamente poderoso.

   ```bash
   [root ~]# cat fruit2.txt
   1       banana      120
   2       grape       500
   3       apple       1230
   4       watermelon  80
   5       orange      400
   [root ~]# awk 'NR==3' fruit2.txt
   3       apple       1230
   [root ~]# awk '{print $2}' fruit2.txt
   banana
   grape
   apple
   watermelon
   orange
   ```

### Gerenciamento de usuários

1. Criar e excluir usuários: `useradd` / `userdel`

   - `-d`: especifique o diretório inicial do usuário ao criar um usuário
   - `-g`: especifique o grupo principal do usuário ao criar um usuário

2. Criar e excluir grupos de usuários: `groupadd` / `groupdel`

   > **Observação**: Os grupos de usuários existem principalmente para facilitar o gerenciamento conjunto de um conjunto de usuários.

3. Alterar senhas: `passwd`

   > **Observação**: Se `passwd` for usado sem especificar um destino, a senha do usuário atual será alterada. Se precisar modificar as senhas de muitos usuários em massa, você pode usar `chpasswd`.

   - `-l` / `-u`: bloquear ou desbloquear um usuário
   - `-d`: limpar a senha de um usuário
   - `-e`: força a expiração imediata da senha, portanto o usuário deverá alterá-la no login
   - `-i`: desabilita o usuário alguns dias após a expiração da senha

4. Visualize e modifique os períodos de validade da senha: `chage`
5. Alternar usuários: `su`
6. Execute comandos como administrador: `sudo`

   > **Observação**: Se for esperado que um usuário execute comandos com privilégios de administrador, esse usuário deverá aparecer na lista `sudoers`. O arquivo `sudoers` reside em `/etc`.

7. Edite o arquivo `sudoers`: `visudo`
8. Exibir informações de usuário e grupo: `id`
9. Enviar mensagens para outros usuários: `write` / `wall`
10. Visualize ou defina se você aceita mensagens de outros usuários: `mesg`

### Sistema de arquivos

#### Arquivos e caminhos

1. Regras de nomenclatura: o comprimento máximo do nome do arquivo depende do tipo de sistema de arquivos, mas geralmente um nome de arquivo não deve exceder 255 caracteres. Embora a maioria dos caracteres possa ser usada em nomes de arquivos, é melhor usar letras maiúsculas e minúsculas, dígitos, sublinhados e pontos em inglês. Os nomes dos arquivos podem conter espaços, mas é melhor evitá-los, pois você precisará de aspas ou de escape.
2. Extensões: as extensões de arquivo são opcionais no Linux, mas nos ajudam a entender o conteúdo do arquivo. Alguns aplicativos reconhecem arquivos por suas extensões, mas muitos não. Comandos como `file` não dependem de extensões.
3. Arquivos ocultos: arquivos cujos nomes começam com um ponto são arquivos ocultos no Linux.

#### Estrutura de diretório

1. `/bin` - arquivos binários para comandos básicos
2. `/boot` - arquivos estáticos para o carregador de boot
3. `/dev` - arquivos do dispositivo
4. `/etc` - arquivos de configuração
5. `/home` - diretório pai para diretórios pessoais de usuários comuns
6. `/lib` - arquivos de biblioteca compartilhada
7. `/lib64` - arquivos de biblioteca compartilhados de 64 bits
8. `/lost+found` - armazena arquivos desvinculados
9. `/media` - pontos de montagem para dispositivos detectados automaticamente
10. `/mnt` - pontos de montagem temporários
11. `/opt` - local de instalação para pacotes complementares opcionais
12. `/proc` - informações do kernel e do processo
13. `/root` - o diretório inicial do superusuário
14. `/run` - dados de tempo de execução exigidos pelo sistema
15. `/sbin` - binários para o superusuário
16. `/sys` - um pseudo sistema de arquivos para dispositivos
17. `/tmp` - arquivos temporários
18. `/usr` - aplicativos do usuário
19. `/var` - dados variáveis

#### Permissões de acesso

1. `chmod` - altera bits de modo de arquivo

   > **Observação**: `chmod` pode alterar as permissões de duas maneiras: notação simbólica e notação numérica. Além de `chmod`, `umask` pode ser usado para especificar quais bits de permissão serão removidos das permissões padrão de novos arquivos.

2. `chown` - alterar proprietário do arquivo
3. `chgrp` - alterar propriedade do grupo

#### Gerenciamento de disco

1. Listar o status de uso do disco do sistema de arquivos: `df`
2. Operações de tabela de partição de disco: `fdisk`
3. Ferramenta de particionamento de disco: `parted`
4. Formate um sistema de arquivos: `mkfs`
5. Verificação do sistema de arquivos: `fsck`
6. Converta ou copie arquivos: `dd`
7. Montar e desmontar: `mount` / `umount`
8. Crie, ative e desative partições swap: `mkswap` / `swapon` / `swapoff`

> **Nota**: Executar os comandos acima traz alguns riscos. Se você não os entende claramente, não os use casualmente. Ao utilizá-los, o melhor é seguir atentamente os materiais de referência e confirmar se realmente pretende realizar a operação.

### Editor: `vim`

1. Inicie `vim`. Você pode iniciá-lo com `vi` ou `vim`. Um nome de arquivo pode ser especificado na inicialização ou você pode nomear o arquivo ao salvar.
2. Modo de comando, modo de inserção e modo de última linha. `vim` inicia no modo de comando. Pressionar `i` entra no modo de inserção. Pressionar `Esc` retorna ao modo de comando. Entrar em `:` no modo de comando entra no modo de última linha, onde `q!` força o encerramento sem salvar. No modo de comando, `v` entra no modo visual, onde uma região pode ser selecionada e operada.
3. Salve e saia: `:wq`, `q!`, `ZZ`, `:w`
4. Operações do cursor
   - `h`, `j`, `k`, `l`
   - `Ctrl+y`, `Ctrl+e`
   - `Ctrl+f`, `Ctrl+b`
   - `G`, `gg` e saltos de linha numérica
5. Operações de texto
   - excluir: `dd`, `d$`, `d0`, `dw`, `:%d`
   - copie e cole: `yy`, `p`
   - desfazer e refazer: `u`, `Ctrl+r`
   - classificar conteúdo: `%!sort`
6. Pesquisar e substituir
   - pesquisando com `/`
   - substituindo por comandos como `:1,$s/doc.*/hello/gice`
7. Configurações no modo de última linha
   - `set ts=4`
   - `set nu` / `set nonu`
   - `syntax on` / `syntax off`
   - `set ruler`
   - `set hls` / `set nohls`

   > **Observação**: Se você deseja que essas configurações tenham efeito sempre que `vim` for iniciado, grave-as em `.vimrc` no diretório inicial do usuário.

8. Truques avançados
   - compare vários arquivos: `vim -d foo.txt bar.txt`
   - abrir vários arquivos
   - dividir janelas com `sp` ou `vs`
   - mapear teclas de atalho com `map` ou `inoremap`
   - grave e reproduza macros com `q`, registros e `@`

### Instalação e configuração de software

#### Usando gerenciadores de pacotes

1. `yum`, atualizador Yellowdog modificado
   - `yum search`
   - `yum list installed`
   - `yum install`
   - `yum remove`
   - `yum update`
   - `yum check-update`
   - `yum info`
2. `rpm`, Gerenciador de Pacotes Red Hat
   - instalar pacotes: `rpm -ivh <packagename>.rpm`
   - remover pacotes: `rpm -e <packagename>`
   - pacotes de consulta: `rpm -qa`

A lição usa Nginx como exemplo de instalação por meio de `yum` e MySQL como exemplo de instalação manual de pacotes RPM. Ele também observa que MySQL e MariaDB têm dependências conflitantes de nível inferior, e é por isso que algumas bibliotecas MariaDB podem precisar ser removidas primeiro.

#### Baixe, extraia e configure variáveis ​​de ambiente

A lição usa o MongoDB como exemplo aqui. O processo básico é baixar o pacote, extraí-lo, adicionar seu diretório `bin` a `PATH`, recarregar a configuração do shell e, em seguida, verificar os binários instalados com comandos de versão.

#### Construir e instalar a partir do código-fonte

1. Instalando Python 3.6
2. Instalando Redis 3.2.12

> **Observação**: Depois de instalar software como Python, muitas vezes ainda é necessário atualizar `PATH`, em `.bash_profile` no diretório inicial do usuário ou em `/etc/profile`. O primeiro se comporta como uma variável de ambiente no nível do usuário, enquanto o último atua como uma variável de ambiente no nível do sistema.

### Configurando serviços

No Linux podemos instalar e configurar vários tipos de serviços, transformando o sistema em um servidor de banco de dados, servidor web, servidor de cache, servidor de arquivos, servidor de fila de mensagens e assim por diante. A maioria dos serviços Linux são configurados como daemons, processos em segundo plano que continuam a ser executados sem impedir que o Linux seja encerrado corretamente. É por isso que muitos nomes de serviços terminam com a letra `d`, abreviação de `daemon`, como `firewalld`, `mysqld` e `httpd`. Após a instalação de um serviço, ele geralmente pode ser iniciado, interrompido e gerenciado de outra forma com `systemctl` ou `service`.

Exemplos da lição:

1. Inicie o serviço de firewall: `systemctl start firewalld`
2. Pare o serviço de firewall: `systemctl stop firewalld`
3. Reinicie o serviço de firewall: `systemctl restart firewalld`
4. Ver o status do firewall: `systemctl status firewalld`
5. Habilite ou desabilite o firewall na inicialização: `systemctl enable firewalld` / `systemctl disable firewalld`

### Tarefas agendadas

1. Execute comandos em um horário especificado

   - `at`: enfileirar uma tarefa para execução em um horário especificado
   - `atq`: visualizar a fila de tarefas pendentes
   - `atrm`: remover uma tarefa na fila

2. Agendamento de tarefas Crontab

`crontab -e` abre o crontab para edição e permite configurar trabalhos agendados com expressões Cron. A lição também aponta que os arquivos cron relacionados residem em `/etc`, e o próprio arquivo `crontab` do sistema inclui comentários explicando os campos Cron.

### Acesso e gerenciamento de rede

1. Conexão remota segura: `ssh`
2. Buscar recursos pela rede: `wget`
3. Enviar e receber e-mail: `mail`
4. Ferramenta de configuração de rede mais antiga: `ifconfig`
5. Ferramenta de configuração de rede mais recente: `ip`
6. Verificações de acessibilidade de rede: `ping`
7. Exibir ou gerenciar tabelas de rotas: `route`
8. Ver serviços de rede e portas: `netstat` / `ss`
9. Capture e inspecione o tráfego: `tcpdump`
10. Cópia segura do arquivo: `scp`
11. Sincronização de arquivos: `rsync`
12. Transferência segura de arquivos: `sftp`

### Gestão de Processos

1. Ver processos: `ps`
2. Exibir a árvore de processos: `pstree`
3. Encontre processos que correspondam às condições fornecidas: `pgrep`
4. Encerre um processo por PID: `kill`
5. Encerre um processo pelo nome: `killall` / `pkill`
6. Coloque um processo em segundo plano
   - `Ctrl+Z`
   - `&`
7. Consultar trabalhos em segundo plano: `jobs`
8. Continuar um trabalho interrompido em segundo plano: `bg`
9. Traga uma tarefa em segundo plano para primeiro plano: `fg`
10. Ajuste a prioridade de tempo de execução: `nice` / `renice`
11. Manter um processo em execução após o logout: `nohup`
12. Rastrear chamadas do sistema: `strace`
13. Visualize o nível de execução atual: `runlevel`
14. Monitore o uso de recursos em tempo real: `top`

### Diagnóstico do sistema

1. Diagnosticar problemas de inicialização: `dmesg`
2. Visualizar informações de atividade do sistema: `sar`
3. Ver uso de memória: `free`
4. Estatísticas de memória virtual: `vmstat`
5. Estatísticas da CPU: `mpstat`
6. Visualize o mapa de memória de um processo: `pmap`
7. Reportar estatísticas de CPU e E/S do dispositivo: `iostat`
8. Exibir todos os dispositivos PCI: `lspci`
9. Exibir o status dos recursos de comunicação entre processos: `ipcs`

### Programação Shell

Conforme mencionado anteriormente, o shell é o aplicativo que conecta o usuário e o sistema operacional. Ele fornece a interface para interação humano-computador. Shell scripts são programas escritos para o shell. Podemos usá-los para gerenciamento de sistema e operações de arquivos. Resumindo, escrever scripts shell é uma habilidade básica para quem usa Linux seriamente.

A Internet contém uma grande quantidade de material de shell script, portanto esta lição não tenta um tratamento completo. Em vez disso, dá vários pequenos exemplos:

1. Leia dois números inteiros, `m` e `n`, e calcule a soma de todos os números inteiros de `m` a `n`.
2. Crie automaticamente uma pasta e um número específico de arquivos.
3. Instale automaticamente uma versão específica do Redis.

### Recursos relacionados

1. Atalhos comuns de linha de comando do Linux

    | Atalho | Descrição |
   | ---------- | -------------------------------------------- |
    | `tab` | preencher automaticamente um comando ou caminho |
    | `Ctrl+a` | mova o cursor para o início da linha |
    | `Ctrl+e` | mova o cursor para o final da linha |
    | `Ctrl+f` | mova o cursor um caractere para a direita |
    | `Ctrl+b` | mova o cursor um caractere para a esquerda |
    | `Ctrl+k` | cortar do cursor até o final da linha |
    | `Ctrl+u` | cortar do cursor até o início da linha |
    | `Ctrl+w` | corte a palavra antes do cursor |
    | `Ctrl+y` | cole o conteúdo cortado anteriormente |
    | `Ctrl+c` | interromper a tarefa atualmente em execução |
    | `Ctrl+h` | exclua o caractere antes do cursor |
    | `Ctrl+d` | sair do shell atual |
    | `Ctrl+r` | histórico de comando de pesquisa |
    | `Ctrl+g` | sair da pesquisa do histórico |
    | `Ctrl+l` | limpe a tela e abra uma nova linha no topo |
    | `Ctrl+s` | bloquear o terminal temporariamente |
    | `Ctrl+q` | desbloquear o terminal |
    | `Ctrl+z` | pare a tarefa atual e coloque-a em segundo plano |
    | `!!` | execute o comando anterior |
    | `!number` | execute o comando de histórico com esse número |
    | `!letter` | execute o comando mais recente começando com essa letra |
    | `!$` / `Esc+.` | recuperar o último argumento do comando anterior |
    | `Esc+b` | ir para o início da palavra atual |
    | `Esc+f` | ir para o final da palavra atual |

2. O que significam as seções de uma página `man`

    | Seção | Descrição |
   | ------------ | ------------------------------------------------------------ |
    | `NAME` | o nome do comando e breve introdução |
    | `SYNOPSIS` | a sintaxe básica do comando |
    | `DESCRIPTION` | uma descrição detalhada do comando e suas opções |
    | `OPTIONS` | explicações sobre opções de linha de comando |
    | `EXAMPLES` | exemplos de uso de referência |
    | `EXIT STATUS` | códigos de saída; normalmente `0` significa sucesso |
    | `SEE ALSO` | outros comandos ou informações relacionadas |
    | `BUGS` | problemas conhecidos relacionados ao comando |
    | `AUTHOR` | informações sobre o autor |
